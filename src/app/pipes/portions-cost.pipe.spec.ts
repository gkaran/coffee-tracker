import { PortionsCostPipe } from './portions-cost.pipe';

describe('PortionsCostPipe', () => {
  it('create an instance', () => {
    const pipe = new PortionsCostPipe();
    expect(pipe).toBeTruthy();
  });
});
