import { DateDistanceInWordsToNowPipe } from './date-distance-in-words-to-now.pipe';

describe('DateDistanceInWordsToNowPipe', () => {
  it('create an instance', () => {
    const pipe = new DateDistanceInWordsToNowPipe();
    expect(pipe).toBeTruthy();
  });
});
